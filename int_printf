#include "main.h"

/**
* _printf - Returns: the number of characters printed (excluding the
null byte used to end output to strings)
write output to stdout, the standard output stream
*/
int _printf(const char *format, ...)
{
int i = 0, j=0;
va_list ap;

function list
